/* eslint-disable no-unused-vars */
/* eslint-disable no-undef */
import * as React from "react";

const Clicky = () => {
  setTimeout(() => {
    const num = number + 1;
    setNumber(num);
  }, 1000);
};

export default Clicky;
